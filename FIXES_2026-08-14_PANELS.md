# Правки панелей и карточек — авторам приложений

**Дата: 14.08.2026 · Основание: [`PANEL_STANDARD.md`](PANEL_STANDARD.md) и дизайн-код витрины
(`extella-toolbar-src/DESIGN_CODE.md`, канон с 31.07.2026, правка контраста от 14.08.2026).**

Кому: авторам «Разработка на Extella», «1С Agent AI», «Recruiter», «Astra Telecom (Demo)».
Что это: конкретный список правок по результатам аудита 14.08. Общая часть — один раз для всех,
дальше — по приложениям. Снапшоты в магазине иммутабельны, поэтому каждая правка едет
**новой версией листинга**.

---

## Общее для всех — проверь у себя, даже если твоего приложения нет в списке

**Шрифты.** Nunito 400–700 — весь интерфейс; Source Serif 4 — только H1; JetBrains Mono —
машинный текст (версии, хэши, статусные строки). Начертания обязаны лежать в самом документе
(`@font-face` с data: URI) — внешние ссылки на Google Fonts и системные шрифты не считаются:
на чужой машине текст уезжает в Georgia. Готовый блок начертаний можно вынуть из витрины
скриптом `extella-toolbar-src/tools/extract_fonts_block.py`.

**Кнопки.** Форма — пилюля (радиус 999px). Залитая — **одна на экран**, остальные контурные.
Заливка — глубокая бронза `#A5632A`, текст белый, ховер `#8F551F`. На светлой бронзе `#C57E33`
текста не бывает — она осталась только для плашек и декора. Вес текста кнопки ≤600.

**Шкалы.** Кегли: 11 / 13 / 15 / 20 / 26. Отступы: 4 / 8 / 12 / 16 / 24 / 32 / 48.
Радиусы: 8 (контролы) / 12 (карточки) / 999 (кнопки). Значений между ступенями не бывает.

**Запрещено.** Тени (глубину даёт граница 1px) · градиенты · эмодзи в интерфейсе
(единственное исключение — аватар агента, выбранный человеком) · третий акцентный цвет
(есть бронза-действие и петроль-система, всё).

**Тексты.**
- Заголовок называет предмет («Базы 1С»), не жанр («Обзор», «Как это работает», «Информация»).
  Справка — одна кнопка на окно, не три формулировки рядом.
- Один контрол — одна фраза: метка ИЛИ плейсхолдер, состояние не дублирует ни то ни другое.
- Пустое состояние: факт + одна кнопка с именем действия.
- Ошибка: что случилось словами человека + какую кнопку нажать. Внутренности — в лог.
- Запрещённые слова в видимом тексте: токен · деплой · юзер · эндпоинт · скоуп · localhost ·
  PID · PORT · воркер · мост · реестр · канал · устройство панели · плейсхолдер · снапшот ·
  инстанс · конфиг.

**Карточка в магазине.** Иконка, описание, теги — обязательны (гейт меты уже в стандартах).
«No description» — дефект выпуска.

**Проверка перед публикацией.**

```bash
python3 extella-toolbar-src/tools/check_panel_canon.py путь/к/panel.html   # должно быть: 0 нарушений
```

Плюс чек-лист из §7 `PANEL_STANDARD.md` — 7 пунктов, две минуты.

---

## «Разработка на Extella» — v3.0.4 → следующая версия

Замер чекером: 5 классов нарушений.

1. Шрифт `Source Sans 3` — это канон **до** 31.07. Перевести на Nunito + Source Serif 4
   (H1) + JetBrains Mono (статусная строка «Готов к проверке…» уже моно — оставить).
   Добавить `button,input,select,textarea{font-family:inherit}`.
2. Две залитые бронзовые кнопки на одном экране — «Скопировать всё руководство» и
   «Подключить Codex». Залитой остаётся первая, «Подключить Codex» — контурная.
3. Кнопки прямоугольные (радиусы 4–6px) → пилюли; заливка `#C57E33` с тёмным текстом →
   `#A5632A` с белым; вес 700 → 600.
4. Шкалы: кегли 12×4, 14×2, 17×2 → на шкалу; радиусы 4×3, 6×3 → 8/12; отступы — 27 значений
   мимо шкалы (лидер 14px×10) → на шкалу.
5. Заголовок блока «Codex ↔ Extella» серифом → Nunito 600, 20px (сериф только у H1).
6. Слово «токен» в видимом тексте → «ключ доступа».

## «1С Agent AI» — v1.1.0 → следующая версия

Панель ставится на устройство, поэтому правки в исходнике панели + перепубликация.

1. Контрол подключения говорит одну фразу дважды: «● Подключение не выбрано Подключение
   не выбрано». Форма: метка **«База 1С»**, в поле — **«Выбери базу»**, состояние — один
   раз и отдельно.
2. Пустое состояние «Подключённых баз пока нет» заканчивается тупиком «Управление
   подключениями» — управлять нечем. Заменить на кнопку **«Подключить базу»**.
3. Ошибка «Не удалось открыть Агент 1С. Extella не сообщила устройство панели» →
   **«Не удалось открыть панель. Открой её с рабочего стола Extella и нажми «Повторить»»**.
4. Метка «состояние канала с 1С» — слово «канал» запрещено → **«связь с 1С»**.
5. Выпадающий список наезжает на контрол, который его открыл. Проверить окно со всеми
   открытыми списками и на ширине 375px — наездов быть не должно.
6. Кнопка «Как это работает?» стоит в ряду с контролом состояния — жанровый текст в рабочей
   зоне. Справка — одна кнопка «Справка» в углу окна.
7. Начертания не доставлены (чекер: ни одного `@font-face`) — панель рисуется системным
   шрифтом. Вшить блок начертаний.
8. Карточка в магазине без описания — заполнить описание и теги.

## «Recruiter» — v0.3.3

1. Иконка листинга — эмодзи-робот на градиенте. Канон запрещает и то и другое: заменить
   на линейную иконку или логотип на плоском фоне (PNG/WEBP до 5 МБ, перезалить через
   «Редактировать карточку»).

## «Astra Telecom (Demo)» — v2.0.0

1. Карточка без описания — заполнить описание и теги. Демо — не освобождение: карточку
   видят все посетители витрины.

---

## Как принимается

Скачиваю страницу новой версии как покупатель, прогоняю чекер (должно быть 0), прохожу
чек-лист §7, сверяю скрин до/после. Вопросы по значениям — в `PANEL_STANDARD.md`, спорное —
Элле.
